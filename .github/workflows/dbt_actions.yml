name: Run dbt on BigQuery using mwhitaker/dbt-action

on:
  push:
    branches: [ "test" ]

jobs:
  run_dbt:
    runs-on: ubuntu-latest
    steps:
    
    - name: dbt-action
      id: dbt-debug
      uses: mwhitaker/dbt-action@master
      with:
        dbt_command: "dbt debug --config-dir"
      env:
        DBT_BIGQUERY_TOKEN: ${{ secrets.DBT_BIGQUERY_TOKEN }}
        
    - name: Get the result
      if: ${{ always() }}
      run: echo "${{ steps.dbt-run.outputs.result }}"
      shell: bash
